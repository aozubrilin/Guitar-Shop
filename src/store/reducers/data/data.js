import { extend } from '../../../utils';
import { ActionType } from '../../action';

const initialState = {};

const data = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { data };
