import { extend } from '../../../utils/utils';
import { ActionType } from '../../actions';

const initialState = {};

const app = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { app };
